import { defineMessages } from 'react-intl';

const messages = defineMessages({
  'code.reminder.email-template.subject': {
    id: 'code.reminder.email-template.subject',
    defaultMessage: 'Reminder on edX course assignment',
  },
});

export default messages;
